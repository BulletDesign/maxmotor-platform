import { requireUser } from "../../_lib/auth.js";
import { handleError, json } from "../../_lib/http.js";
export async function onRequestGet({ request, env }) { try { const user=await requireUser(request,env.DB,["customer"]); const balance=await env.DB.prepare("SELECT COALESCE(SUM(points),0)-(SELECT COALESCE(SUM(points_reserved),0) FROM redemptions WHERE user_id=?1 AND status='requested') balance FROM points_ledger WHERE user_id=?1").bind(user.id).first(); const movements=await env.DB.prepare("SELECT movement_type,points,description,created_at FROM points_ledger WHERE user_id=?1 ORDER BY created_at DESC LIMIT 50").bind(user.id).all(); const invoices=await env.DB.prepare("SELECT (SELECT COUNT(*) FROM invoices WHERE user_id=?1) total,(SELECT COALESCE(SUM(amount_cents),0) FROM invoices WHERE user_id=?1)+(SELECT COALESCE(SUM(cash_after_points_cents),0) FROM redemptions WHERE user_id=?1 AND status='claimed') amountCents").bind(user.id).first(); return json({ balance:balance.balance,movements:movements.results||[],invoiceCount:invoices.total,invoiceAmountCents:invoices.amountCents }); } catch(error){ return handleError(error); } }
