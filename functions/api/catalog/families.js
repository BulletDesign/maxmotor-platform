import { requireUser } from "../../_lib/auth.js";
import { assertSameOrigin,handleError,HttpError,json,readJson } from "../../_lib/http.js";
export async function onRequestPost({request,env}){try{assertSameOrigin(request);const actor=await requireUser(request,env.DB,["superadmin"]);const body=await readJson(request);const name=String(body.name||"").trim();if(name.length<3)throw new HttpError(400,"Nombre de familia requerido");const id=crypto.randomUUID();await env.DB.batch([env.DB.prepare("INSERT INTO product_families(id,name) VALUES(?1,?2)").bind(id,name),env.DB.prepare("INSERT INTO audit_log(id,actor_user_id,action,entity_type,entity_id) VALUES(?1,?2,'family.create','product_family',?3)").bind(crypto.randomUUID(),actor.id,id)]);return json({id},201);}catch(error){return handleError(error);}}
